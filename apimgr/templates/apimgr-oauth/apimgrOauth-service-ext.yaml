{{- if eq .Values.apimgr.Oauth.enabled true }}
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ .Values.apimgr.Oauth.name }}
  name: {{ .Values.apimgr.Oauth.name }}-ext
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.apimgr.name }}
  ports:
  - port: {{ .Values.apimgr.Oauth.trafficPort }}
    protocol: TCP
    nodePort: {{ .Values.apimgr.Oauth.nodePort }}
{{- end }}
